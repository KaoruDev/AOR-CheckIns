=render "layouts/nav"
.login
  - if devise_mapping.omniauthable?
    .login-button
      = link_to omniauth_authorize_path(resource_name, :twitter) do
        .twitter-icon
          =fa_icon "twitter"
        .login-text
          Log in with Twitter